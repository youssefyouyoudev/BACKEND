[Definition]
failregex = ^<HOST> .* "(?:GET|POST|PUT|PATCH|DELETE) /(?:\.env|\.git|vendor|storage/logs|backup|database).*" (?:403|404) .*
            ^<HOST> .* "(?:GET|POST) /(?:admin/login|api/auth/login).*" 429 .*
            ^<HOST> .* "(?:GET|POST) /(?:stream|bridge|go|bridge-channel)/.*" 429 .*
ignoreregex =
